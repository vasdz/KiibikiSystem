# 🛡️ КиИБик.SYS (Банк Кибиков)

**КиИБик.SYS** — это полнофункциональная веб-система цифрового банка и программы лояльности для кафедры Информационной Безопасности. Проект позволяет вести учет внутренней валюты («Кибиков»), управлять транзакциями, публиковать новости и взаимодействовать со студентами через современный веб-интерфейс.

 
*(Замени ссылку на свой скриншот, если хочешь)*

---

## 🚀 Ключевые возможности

*   **Аутентификация и Авторизация:** Безопасная регистрация и вход в систему с использованием JWT-токенов.
*   **Ролевая модель доступа (RBAC):** Четкое разделение прав между `Студентами` и `Администраторами`.
*   **Цифровой кошелек:** У каждого пользователя есть баланс "Кибиков".
*   **Административная панель:**
    *   Начисление баллов любому пользователю с указанием причины.
    *   Создание, редактирование и удаление новостей/событий в ленте.
    *   Возможность загрузки изображений к постам.
*   **Студенческий кабинет:**
    *   Просмотр актуального баланса и истории транзакций.
    *   Просмотр ленты событий кафедры.
    *   Редактирование своего профиля.
    *   Загрузка файлов (например, подтверждение достижений) на проверку.
*   **Безопасность:** Встроенные механизмы защиты от веб-уязвимостей (CSP, HSTS, X-Frame-Options) и ограничение частоты запросов (Rate Limiting).
*   **Контейнеризация:** Полная изоляция и простота развертывания благодаря Docker и Docker Compose.

---

## 🛠️ Стек технологий

| Категория          | Технология                                                              |
| ------------------ | ----------------------------------------------------------------------- |
| **Backend**        | Python 3.12, FastAPI, SQLAlchemy, SQLModel, Pydantic, Alembic (миграции)|
| **Frontend**       | React.js, TypeScript, Vite, TailwindCSS, Axios                          |
| **База данных**    | PostgreSQL 15                                                           |
| **Инфраструктура** | Docker, Docker Compose                                                  |
| **Безопасность**   | JWT (python-jose), bcrypt (passlib), SlowAPI (Rate Limiting)            |

---

## ⚙️ Установка и запуск

Для запуска проекта на локальной машине требуется **Docker** и **Docker Compose**.

#### 1. Клонирование репозитория

git clone https://github.com/vasdz/KiibikiSystem.git
cd KiibikiSystem

#### 2. Конфигурация Backend

В корне проекта создайте файл `.env` на основе примера `.env.example`.

Для Windows
copy .env.example .env

Для Linux/macOS
cp .env.example .env

Файл `.env` должен выглядеть так:
.env
PROJECT_NAME="Kiibiki System"
API_V1_STR=/api/v1

Подключение к БД
POSTGRES_USER=kiibiki_admin
POSTGRES_PASSWORD=secure_pass
POSTGRES_DB=kiibiki_core
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

Настройки JWT
SECRET_KEY=СЮДА_ВСТАВЬ_СВОЙ_ОЧЕНЬ_СЕКРЕТНЫЙ_КЛЮЧ
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440 # 1 день

#### 3. Сборка и запуск контейнеров

Выполните команду в корне проекта:

docker compose up --build -d

Эта команда соберет образы для бэкенда и фронтенда, запустит контейнеры и создаст базу данных.

#### 4. Применение миграций базы данных

После первого запуска контейнеров необходимо создать таблицы в базе данных.

docker compose exec backend alembic upgrade head

*(Если вы создавали таблицы вручную, используйте `alembic stamp head`, чтобы синхронизировать историю миграций)*.

---

## 🕹️ Как использовать

Приложение будет доступно по следующим адресам:
*   **Frontend (Дэшборд):** [http://localhost:5173](http://localhost:5173)
*   **Backend (API Docs):** [http://localhost:8000/docs](http://localhost:8000/docs)

#### Учетные данные

1.  Зарегистрируйте нового пользователя `admin` через интерфейс регистрации.
2.  Чтобы выдать ему права администратора, выполните SQL-запрос:
    ```
    docker compose exec db psql -U kiibiki_admin -d kiibiki_core -c "UPDATE users SET role = 'ADMIN' WHERE username = 'admin';"
    ```
3.  Войдите в систему под пользователем `admin`.

Теперь у вас будут доступны все административные функции.

---

## 👨‍💻 Автор vasdz

*   **[Лабин Александр]**
*   **GitHub:** [https://github.com/vasdz](https://github.com/vasdz/)
